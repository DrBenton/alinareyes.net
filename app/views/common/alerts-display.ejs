<% var errors = req.flash('errors') %>
<% if (!!errors && errors.length > 0) { %>
    <ul class="errors">
    <% errors.forEach(function (errorMsg) { %>
        <li><%= errorMsg %></li>
    <% }) %>
    </ul>
<% } %>